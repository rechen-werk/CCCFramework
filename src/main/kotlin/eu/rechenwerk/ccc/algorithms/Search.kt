package eu.rechenwerk.ccc.algorithms

class Search<V>(tree: Graph<V>, algorithm: Algorithm) {
    fun find(value: V): Node<T> {
        return Node(value)
    }

    private findBFS(value: V)
}

private sealed class Queue<V>(graph: Graph<V>, algorithm: Algorithm) {
    fun add(node: V) {

    }
}